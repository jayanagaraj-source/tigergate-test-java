apiVersion: apps/v1
kind: Deployment
metadata:
  name: tigergate-test-java
spec:
  replicas: 1
  selector:
    matchLabels: { app: tigergate-test-java }
  template:
    metadata:
      labels: { app: tigergate-test-java }
    spec:
      hostNetwork: true # Deliberately insecure IaC fixture.
      containers:
        - name: app
          image: tigergate-test-java:latest
          securityContext:
            privileged: true
            runAsUser: 0
          env:
            - name: DATABASE_PASSWORD
              value: "fixture-password-not-real"
